# Development Workflow — MyFinanceView

> **This file is a redirect stub.** The OpenSpec workflow it described was retired on 2026-06-25
> and is archived under `archive/openspec-legacy/`.

The active workflow is the **Uncle Bob harness**. Start here:

- **[AGENTS.md](../AGENTS.md)** — navigation map: which agent does what, when to use the
  harness vs. direct work.
- **[docs/uncle-bob/workflow.md](uncle-bob/workflow.md)** — full pipeline:
  `spec_partner → gherkin_author → ⏸ human gate → tdd_craftsman → judge → mutation_tester → done`.

The retired OpenSpec tooling (commands, directory layout, preflight script, cheatsheet) is
preserved as historical record in `archive/openspec-legacy/` and is no longer authoritative.
