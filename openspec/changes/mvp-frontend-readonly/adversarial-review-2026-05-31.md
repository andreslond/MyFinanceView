## Adversarial Review · mvp-frontend-readonly · 2026-06-01

**Verdict:** FAIL

**Scope reviewed:**
- 4 OpenSpec artifacts (proposal.md, design.md, specs/frontend-mvp/spec.md, specs/myfinance-data-policies/spec.md)
- tasks.md (65 numbered tasks across 3 branches)
- Cross-referenced against: SPEC.md, docs/base-standards.md, docs/backend-standards.md, docs/data-model.md, docs/frontend-standards.md, docs/api-spec.yml, docs/workflow.md, existing migrations V001–V003, parallel changes (supabase-backup-policy in `worktrees/supabase-cuts` and `worktrees/supabase-replant`)

---

### Findings

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| B1 | Blocker | Data model | `specs/myfinance-data-policies/spec.md:49,52-53,56-57` — trigger writes to `myfinance.merchants` but that table does NOT exist yet. `docs/data-model.md §3 V007` lists `CREATE TABLE myfinance.merchants` as a PENDING migration (TASK-DB-04). Existing migrations under `backend/database/migrations/` stop at V003. `tasks.md §1` reserves only `V{n}` (RLS) and `V{n+1}` (trigger) — no task creates `merchants`. The trigger will fail to compile (`relation "myfinance.merchants" does not exist`). | spec: add a migration that creates `merchants` (effectively V004 = the pending TASK-DB-04) as a hard precondition for the trigger migration; OR explicitly add the merchants creation to branch #1. Renumber the trigger and add tasks 1.X to define the schema. Cross-reference `data-model.md §3 V007` and confirm the column list and seed. |
| B2 | Blocker | Data model | `specs/myfinance-data-policies/spec.md:46-65` — trigger references `transactions.category_confirmed`, but per `docs/data-model.md §3 V008` (TASK-DB-05) that column is PENDING and will be added in V008. V001 has no such column. The `tasks.md §1` migration plan does not add it. The trigger DDL will fail with `column "category_confirmed" does not exist`. | spec: declare a hard dependency on V008 (TASK-DB-05) landing first. Either (a) include the V008 migration in branch #1 as a precondition, or (b) refuse to start until TASK-DB-05 lands. Mark this as Gate-A architecture decision: ship the MVP without the trigger and only the categorization columns, OR fold V007+V008 into this change. |
| B3 | Blocker | Migrations | `backend/database/migrations/V002__rls_policies.sql:18-198` ALREADY enables RLS and creates `SELECT/INSERT/UPDATE/DELETE` policies for `banks`, `user_settings`, `categories`, `accounts`, `transactions`, `budgets`, `budget_categories`, `exchange_rates`. `tasks.md §1.3, §1.4` propose to create a new `V{n}__rls_myfinance_policies.sql` that does the same thing — running it will fail with `policy "..._select_own" for relation "myfinance.transactions" already exists`. The proposal/design totally miss the existing V002 file and the existing canonical policies. | spec: rewrite the design — there is no need for a new RLS migration. The work reduces to (a) verifying V002 against this spec's scenarios, (b) adding policies ONLY for the not-yet-existing `merchants` table inside its V007 migration. Delete tasks 1.3–1.4. Optionally add `DROP POLICY IF EXISTS ... ; CREATE POLICY` if a re-shape is needed, but the design must justify why. Also: scenario "Lectura anónima rechazada" (spec.md:36) requires `categories` SELECT to fail anonymously, but V002 line 68-71 policy `categories_select_system_and_own` is restricted `TO authenticated` only — verify this matches scenario semantics. |
| B4 | Blocker | Authz / trigger | `design.md:58` and `specs/myfinance-data-policies/spec.md:73` choose `SECURITY INVOKER` for the trigger function so `auth.uid() = user_id` enforces RLS on the merchant write. But the trigger fires on UPDATE of `transactions.category_id/category_confirmed` — when the n8n pipeline (using `service_role`) updates transactions via `category_confirmed = true` (e.g. webhook feedback from Telegram per SPEC.md §5.5 and §7), `auth.uid()` is NULL, so the WITH CHECK policy `auth.uid() = user_id` BLOCKS the merchant write. The trigger silently fails (or aborts the parent UPDATE) for every n8n / backend-Java path. This breaks SPEC.md §7's "Loop de retroalimentación" the moment the Telegram feedback path or the future Java endpoint runs. | spec: change Decision D2 — either (a) use `SECURITY DEFINER` and have the function explicitly set `user_id := NEW.user_id` (no `auth.uid()` reliance, no RLS bypass risk because the function is the only writer), or (b) drop the RLS scenario "RLS respetada por el trigger" and document that the trigger writes are an internal invariant maintained by the DB, with table-level guard that the merchant `user_id` always equals `NEW.user_id`. Add a scenario for the service_role + n8n path explicitly. |
| B5 | Blocker | Trigger semantics drift | `SPEC.md §7` line 449 says `confidence += 0.1` on every confirmation — flat increment. `specs/myfinance-data-policies/spec.md:53` says first-confirmation = `confidence = 0.6` (0.5 base + 0.1 ajuste), and line 57 says subsequent = `LEAST(0.95, confidence + 0.1)`. The 0.95 cap is NEW behaviour not in SPEC.md §7. The first-confirmation 0.6 derivation assumes the V007 default of 0.5 applies BEFORE the increment, but in the INSERT case the trigger inserts a brand-new row — there is no prior default to "add to". The spec must either (a) state that the trigger INSERTs with `confidence = 0.6` directly, or (b) the math becomes inconsistent. Worse: `data-model.md §3 V007` says `confidence numeric DEFAULT 0.5` but doesn't cap — so any other writer (jOOQ from backend) could legitimately set confidence above 0.95, and the next trigger fire would then floor it via LEAST. Pick one. | spec: amend SPEC.md §7 to include the 0.95 cap (this is a real semantic change to the project north star) OR remove the cap from the trigger and document why the trigger and the eventual Java service must agree. State the INSERT-case confidence explicitly. Add a scenario for the boundary: when stored confidence is already 0.95, next confirmation leaves it at 0.95 and only increments match_count. |
| B6 | Blocker | Forward-compat drift | `proposal.md:12` and `design.md:74` claim DTOs are "alineados con `docs/api-spec.yml`". But `docs/api-spec.yml:42-69` is an empty placeholder — only `/actuator/health` is defined, plus a comment listing future endpoints. NO `TransactionDTO`, `AccountDTO`, `CategoryDTO`, `MerchantDTO` schemas exist. The frontend will invent shapes today that the Java backend may not adopt tomorrow. The "1-2 days swap, no semantic drift" claim has no contract to anchor against. | spec: choose one — (a) include in this change a Gate-A decision to populate `api-spec.yml` schemas for the four DTOs FIRST (in branch #1 or a new branch #0), then build the frontend against them; OR (b) drop the "alineados con api-spec.yml" wording and acknowledge the contract is being invented in the frontend, to be back-aligned when the Java backend lands. Right now the proposal claims an alignment that the artifacts cannot deliver. |
| B7 | Blocker | Workflow / process | `proposal.md` and `design.md` have NO `## Threat model` section. `docs/workflow.md §2 Gate C` (line 144) makes this MANDATORY: "every `proposal.md` MUST declare a `## Threat model` section naming the adversary and what's *out of scope*. Findings out of scope are auto-rejected." Without it, adversarial review has no rejection bar and Gate C cannot pass. | spec: add a `## Threat model` section to `proposal.md` (probably: adversary = leaked anon key + opportunistic web attacker; out of scope = compromised user laptop, compromised Supabase platform, side-channel on JWT). This is a hard prerequisite for /opsx:apply per the workflow doc. |

---

### Major findings

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| M1 | Major | Data shape / business logic in frontend | `proposal.md:9` says "lista de transacciones del ciclo actual con filtros". "Ciclo actual" REQUIRES knowing the cut day (`accounts.cut_day`, PENDING per V005/TASK-DB-03 — not yet migrated) and computing `start, end` via `myfinance.get_billing_period()` (also pending V005). If the frontend computes "ciclo actual" itself it (a) violates `docs/base-standards.md §2` ("frontend nunca computa agregaciones de plata ni valida invariantes de negocio") because cycle calc is a business invariant, and (b) silently produces wrong dates because cut_day is unknown. `specs/frontend-mvp/spec.md` Requirement "Transactions listing screen" (line 63-94) DOES NOT mention "ciclo actual" — it lists "transacciones más recientes" with `occurredAt DESC` and account/category/confirmed filters. Either the proposal or the spec is wrong. | spec: reconcile. Either (a) drop "del ciclo actual" from proposal — the MVP shows recent transactions, end of story; OR (b) declare a hard dependency on V005 + `get_billing_period()` (TASK-DB-03) and add a backend SQL view or RPC for the cycle window. The frontend MUST NOT compute the cycle from cut_day. |
| M2 | Major | Coordination with parallel changes | Two active worktrees exist (`supabase-cuts`, `supabase-replant`) both carrying `supabase-backup-policy` change. That change ships `traefik/dynamic/myfinance-preop.yml`, a backup runner sidecar, retention policy. It DOES NOT touch RLS or merchants. However: it does mandate (per its Decision 8 and proposal) that any write against Supabase remote should be preceded by a verified pre-op snapshot within 60 min. The migration plan in this change (tasks 1.16: "Aplicar las migraciones al proyecto Supabase productivo vía MCP `apply_migration`") is a write, and there is no acknowledgement of the backup-policy gate. If `supabase-backup-policy` lands first, this change's deploy MUST run the documented pre-op procedure. | spec: add a precondition task to §1: "1.15a — verify status of `supabase-backup-policy`; if archived, follow `docs/development-guide.md` pre-op snapshot procedure before applying migration to production". Document the dependency in design.md "Risks / Trade-offs". Coordinate via Notion or commit message. |
| M3 | Major | RLS gap | `specs/myfinance-data-policies/spec.md:5` lists `transactions, accounts, merchants, user_settings, budgets, budget_categories` as user-owned RLS targets. Missing from list: `savings_goals` and `savings_goal_contributions` (per `data-model.md §3 V009`, savings goals are in flight via `plans/savings-goals-plan.md`). If V009 lands during the lifetime of this change, this spec leaves those tables exposed. Even if V009 ships its own RLS, the project rule (D5 Razón: "Aplicar a todo el schema cierra la deuda") explicitly demands coverage of everything. | spec: either explicitly defer savings_goals to the V009 migration with a cross-reference, or include the policies here for forward-completeness with `IF EXISTS` guards. |
| M4 | Major | Trigger semantics — `raw_pattern` undocumented | `design.md:58` and `specs/myfinance-data-policies/spec.md:75-85` say the trigger replicates n8n's normalization for `raw_pattern`/`normalized_name`. The open question at `design.md:158` admits the algorithm is NOT documented in the repo. The fixture-based test (`tasks.md §1.15`) needs a known n8n output set, which requires inspecting the n8n workflow (auth-walled per the review brief). Without the algorithm, the test for "raw_pattern matches n8n" is unverifiable AND the trigger creates merchants with patterns that won't match the LLM's existing lookup logic — breaking SPEC.md §7's "categorize directly if confidence >= 0.85" path. | spec: make `raw_pattern` extraction an explicit Gate-A decision. Options: (a) extract algorithm from n8n NOW (manual prereq, blocks branch #1); (b) freeze `raw_pattern = description` (no normalization — write a downstream task to reconcile); (c) defer the trigger entirely to Épica 3 backend. Pick one before /opsx:apply or branch #1 will stall on this. |
| M5 | Major | Spec verifiability — magic link | `specs/frontend-mvp/spec.md:27-29` "Login con magic link" scenario WHEN/THEN requires a real email to arrive and be clicked. There is no test infrastructure in the spec to fake this (no email-receive helper, no mock for Supabase auth redirect callback). The scenario is unverifiable as written — it asserts behaviour of Supabase + email infra, not of the frontend. | spec: split scenario into two: (1) "user clicks 'Send magic link' → form shows confirmation state" (frontend-verifiable) and (2) "magic link callback URL with valid token → user lands authenticated" (verifiable by simulating the callback route with a token). Drop the email-arrival assertion or move it to manual smoke. |
| M6 | Major | Tests — RLS coverage | `tasks.md §1.9` covers `transactions/accounts/merchants` RLS isolation but NOT `user_settings`, `budgets`, `budget_categories`, `categories`, `banks`, `exchange_rates`. The spec asserts RLS on all of these (D5, `specs/myfinance-data-policies/spec.md:5-29`). A test that only covers 3 of 9 tables is insufficient for the requirement. Worse: the project rule (`docs/base-standards.md §5`) prohibits mocking the database — these RLS tests MUST be Testcontainers against the migration set + a real PostgREST-style JWT, which is non-trivial test plumbing not currently in the backend. | tests: expand 1.9 to cover all 9 tables with a parameterized test. Acknowledge that Testcontainers + RLS testing requires a JWT-issuing helper (Supabase doesn't run inside Testcontainers — only Postgres does; `auth.uid()` is a Supabase function that needs the GUC `request.jwt.claims` set on the session). Add a task to introduce this helper before 1.9 can run. |
| M7 | Major | Tests — DB mocking anti-pattern | `tasks.md §3.6, 3.10, 3.11` describe unit/integration tests of React components that "mock Supabase". This is consistent with frontend unit testing practice (Supabase is an HTTP client there, not the DB), so it is NOT the project's "no mocking the database" anti-pattern per se. HOWEVER: tasks 1.9–1.15 describe DB-level tests where the backend rule applies; the spec must be explicit that the merchant/RLS tests use Testcontainers Postgres + a JWT-claim-setting helper, not a mock for `supabase.from(...).select(...)`. Right now the tasks are ambiguous. | tests: in tasks 1.9–1.15, explicitly note "Testcontainers Postgres, with `SET LOCAL request.jwt.claims = '{...}'` to simulate auth.uid()". Distinguish that pattern from the frontend mock pattern in §3. |
| M8 | Major | Architectural rule violation | `design.md:14` "Cero pérdida del invariante de aprendizaje de `merchants` cuando se cambia categoría desde la UI" + Decision D2 placing the feedback loop in a Postgres trigger violates `SPEC.md §🏛️` "Monolito modular por dominio" — the merchant feedback loop is part of `domain/merchant` (per `docs/backend-standards.md §2` package layout: "merchant/ ← MerchantService + feedback loop"). Putting business logic in a DB trigger creates the EXACT split-brain the architecture forbids: when TASK-BE-06 lands (PATCH /transactions/{id}/category + feedback loop to merchants — SPEC.md §6 Épica 3), the Java service and the trigger BOTH compute the feedback. Either the trigger fires twice and double-increments, or the backend must remember to disable the trigger. This is a structural debt the design.md hand-waves with "el backend Java puede mantenerlo o reemplazarlo — decisión separada". That deferral IS the problem. | spec: make this an explicit Gate-A decision. Options: (a) trigger lives forever, backend Java NEVER does the feedback in code (the trigger is the canonical writer) — requires SPEC.md amendment, since §7 currently describes backend code doing it; (b) trigger lives for MVP only, with a documented removal task in TASK-BE-06; (c) skip the trigger for MVP, accept that UI category changes don't update merchants until backend lands. The current "we'll figure it out later" is not acceptable per `docs/workflow.md §2 Gate A`. |
| M9 | Major | Authz / trust boundary | `proposal.md:9` and `design.md:120` ("Vercel anon key expuesta — la `anon key` es pública por diseño; RLS es la defensa") accept that the anon key ships in the Vite bundle. This is correct for Supabase, BUT: anything in `services/transactionsService.ts` that runs `supabase.from('transactions').update({ category_id, category_confirmed: true })` runs with the user's JWT. If the user JWT is compromised (e.g. via stolen localStorage), the attacker can mass-mutate ALL transactions of that user including writing arbitrary category_id values (including `NULL` if not constrained). The proposal does not declare this in a threat model (cf. B7) and does not require the JWT to be stored httpOnly or to have short TTL. Default Supabase JWT TTL is 1 hour, refresh token has long life. | spec: in the new `## Threat model` section, declare the stolen-JWT threat as accepted with rationale (single-user app, the operator will sign out on shared devices). Document the Supabase JWT TTL setting. Confirm that `category_id` has a NOT NULL constraint or RLS-side guard that an `UPDATE ... SET category_id = NULL` is rejected. If not, add a CHECK constraint or trigger guard. |
| M10 | Major | Idempotency contract drift | `specs/myfinance-data-policies/spec.md:60-65` "Confirmación con misma categoría sigue acumulando" says the trigger does NOT fire on re-confirmation. Then `specs/frontend-mvp/spec.md:107-109` "Confirmación sin cambio" says the UI emits the UPDATE anyway, and the trigger "se dispara igual porque la confirmación es la transición relevante". These contradict: data-policies says no trigger fire when `category_confirmed` was already true AND `category_id` did not change; frontend spec says the trigger fires regardless. Operationally, if the row was already `category_confirmed = true` and you UPDATE with the same values, neither `category_confirmed` nor `category_id` transitions, so the WHEN clause fails → NO trigger fire. The frontend spec is wrong, OR the data-policies WHEN clause is wrong. | spec: pick one. If "re-confirm should fire", remove the `IS DISTINCT FROM` predicate (and accept inflation risk that data-policies scenario explicitly rejects). If "re-confirm should NOT fire", fix `specs/frontend-mvp/spec.md` line 109 to say "el trigger NO se dispara porque no hubo transición". The current contradiction will burn an implementer hour. |

---

### Minor findings

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| m1 | Minor | Process-tooling — progress.md | `progress.md:15` has `last_updated: "2026-06-01T04:55:27Z"` and `current_task: none`, `next_step: "ready for /opsx:apply"`. The file is technically fresh, but the spec.md, design.md, proposal.md, tasks.md were all written in the same `/opsx:propose` session and the gate states (A, B passed?) are not recorded anywhere. Per `docs/workflow.md §2 Gate A` and B, decisions and task-plan approval should be reflected in progress.md or design.md. | process: before /opsx:apply, update progress.md with `decisions_pending_design_update: [B7 threat model, M8 trigger ownership, M4 raw_pattern algorithm]` to surface the open Gate-A decisions identified by this review. |
| m2 | Minor | Scope-creep risk | `tasks.md §3.19` "Documentar la URL productiva, las credenciales y las variables de entorno en `docs/development-guide.md`". Credentials in a markdown file under `docs/` is a footgun — operator could commit env values by accident. `docs/base-standards.md §7` says "secrets via env vars / Supabase Vault. Never in repo." | tasks: rewrite 3.19 — document the URL and which env var names are required, NOT their values. Reference Vercel dashboard for the actual values. |
| m3 | Minor | Spec drift — frontend never aggregates | `specs/frontend-mvp/spec.md:147-157` "Frontend never aggregates money" requirement is well written. But `specs/frontend-mvp/spec.md:131-145` does not assert any aggregate-free invariant in the deployed bundle. A future agent could add a "página total" feature in a follow-up and the lint passes. | tests: add a build-time check that scans `src/` for `.reduce(` calls on monetary fields, similar to the isolation.spec.ts plan. Or accept that this is a code-review discipline. Note in AGENTS.md. |
| m4 | Minor | Vague test outcomes | `tasks.md §3.17` "Smoke-test post-deploy — `curl` a la URL productiva responde HTTP 200; login con el usuario real funciona; lista de transacciones muestra datos reales". The last clause "muestra datos reales" is not falsifiable — what counts as "real"? Number of rows > 0? Specific transaction visible? | tasks: tighten — "first page returns ≥ 25 rows when the user has ≥ 25 transactions and visually the most recent occurredAt is within the last 7 days". |
| m5 | Minor | Spec — paginated total | `specs/frontend-mvp/spec.md:83-89` "Paginación" scenario asserts `?page=2` shows the next block, but the page size (25 per scenario "Lista por defecto" at line 67-69) is hardcoded only there. No scenario asserts max page size, no scenario asserts that total pages or total count is exposed (or NOT exposed) — important because PostgREST `count=exact` is a separate header. | spec: add scenario "Total exposed in response" OR "Total NOT exposed" so the implementer knows whether the UI shows "página 2 de N". |
| m6 | Minor | Spec — categoria_confirmed legacy | `data-model.md §3 V008` introduces `category_confirmed boolean NOT NULL DEFAULT false`. The frontend Modal scenario `specs/frontend-mvp/spec.md:105` UPDATEs the field. But how does the frontend distinguish "AI-confirmed at ingest" vs "user-confirmed in UI"? `category_confirmed = true` from n8n LLM auto-categorization at `confidence >= 0.85` would mean the modal can't distinguish. SPEC.md §7 reuses confidence as the source of truth, not `category_confirmed`. | spec: clarify whether `category_confirmed` ever gets set to true by n8n/LLM (per SPEC.md §7 it's set after USER confirms, not LLM), OR add a scenario that asserts the value is ONLY set true by the user. |
| m7 | Minor | Missing supabase MCP precondition | `tasks.md §3.14-3.16` use the Vercel MCP for deploy, and §1.16 uses Supabase MCP for `apply_migration`. The change does NOT call out that the Supabase MCP must be authenticated in the agent context. If the operator runs /opsx:apply in a fresh session without MCP creds, branch #1 will block at task 1.16. | tasks: add preconditions section listing required MCP servers and how to verify they are connected (`list_projects` returns the expected project ID). |
| m8 | Minor | Tailwind tokens | `tasks.md §2.4` "alinear tokens con `docs/design/design-system.md`". The actual mapping (palette tokens → Tailwind config keys) is not specified. Two implementers will produce two different configs. | spec: add a scenario in `frontend-mvp/spec.md` that asserts named palette mappings (`bg-surface-base`, `text-content-primary`, etc.) match the design-system token list, or just add a sub-task with the explicit mapping. |
| m9 | Minor | ESLint enforcement coverage | `tasks.md §2.16-2.17` cover ESLint + a Vitest static glob test. Good defense in depth. BUT neither task says CI enforces them. `proposal.md` describes Vercel deploy; Vercel will not run vitest unless `npm run build` runs it. If the build skips tests, the static check never runs in CI. | tasks: add a task to put `npm run typecheck && npm run lint && npm run test && npm run build` into a single CI gate (Vercel "Build Command" can be modified to chain these). |
| m10 | Minor | Naming convention | `tasks.md §1.7` names trigger function `myfinance.fn_update_merchant_from_transaction()`. SPEC.md and existing convention use snake_case prefixes (`trigger_set_updated_at` per data-model.md §1) — `fn_` prefix is fine but not established; project convention is unwritten. | spec: pick one prefix convention and document it in `docs/data-model.md §1`. Minor but worth aligning for the future flyway-migrations work. |

---

### Question / Assumption

| # | Severity | Area | Evidence | Question |
|---|---|---|---|---|
| Q1 | Question | Spec drift — payment_day vs cut_day | The proposal does not mention `cut_day` or `payment_day`, yet "ciclo actual" implicitly depends on them. Is the MVP supposed to render the cycle window in the UI at all? If not, M1 collapses to a Minor. If yes, V005 (TASK-DB-03) must land first AND Black Bancolombia / Bancolombia débito cut_day must be discovered (currently unknown per `data-model.md §6`). | clarify in proposal what the user actually sees on the screen — is it "todas las transacciones recientes" or "este corte"? |
| Q2 | Question | Trigger semantics — UPSERT vs explicit INSERT | `design.md:58` says "hace UPSERT del merchant si no existía". `specs/myfinance-data-policies/spec.md:53` describes the INSERT shape with explicit confidence=0.6. PostgREST/PL/pgSQL UPSERT semantics differ from a plain INSERT...ON CONFLICT for confidence math. Which one is the spec? | clarify whether trigger does (a) `INSERT INTO merchants ... ON CONFLICT (user_id, raw_pattern) DO UPDATE SET confidence = LEAST(0.95, merchants.confidence + 0.1), match_count = merchants.match_count + 1, last_confirmed_at = now(), category_id = EXCLUDED.category_id`, or (b) IF EXISTS UPDATE ELSE INSERT, with subtly different concurrency semantics. |
| Q3 | Question | Spec — sign-up flow | `design.md:159` defaults to "no Sign up in MVP, only Login". `specs/frontend-mvp/spec.md` Requirement "Authentication via Supabase Auth" does NOT specify whether `@supabase/auth-ui-react` is configured `view="sign_in"` only or `view="sign_in_or_sign_up"`. If left default, the prebuilt UI shows Sign Up — directly contradicting the design decision. | clarify the auth-ui props in the spec OR add a scenario "Sign up tab not shown". |
| Q4 | Question | Spec — categories user_id NULL handling | V002 `categories_select_system_and_own` policy allows reading rows with `user_id IS NULL` (system categories) OR `auth.uid() = user_id`. `specs/myfinance-data-policies/spec.md:29-46` (Read-only RLS on shared catalogs) treats `categories` as a pure catalog and forbids user writes. This contradicts V002 lines 73-87 which allow user INSERT/UPDATE/DELETE of own categories (where user_id IS NOT NULL). Which one is correct for the MVP? | reconcile: do users have personal categories in the MVP or not? If yes, the spec scenarios at 39-41 are wrong (writes ARE allowed for own rows). If no, V002 must be tightened. |
| Q5 | Question | Spec verifiability — "displayName fallback" | `specs/frontend-mvp/spec.md:127-129` "Fallback" requires `console.warn` on null displayName. Console assertions in Vitest require spying on `console.warn` — straightforward but not free. Is the console warning a real product requirement or theater? Real impact would be a Sentry-level alert, which the MVP doesn't have. | clarify whether the warning is for the operator's eyeball during dev OR a hard product requirement. Default: drop the warning, just render the fallback. |

---

### Acceptance criteria coverage (sampling)

| Requirement | Tested? |
|---|---|
| ✅ frontend-mvp: Frontend stack and runtime | Covered by build/typecheck/tailwind scenarios + tasks 2.2–2.4 |
| ⚠️ frontend-mvp: Authentication via Supabase Auth | Magic-link scenario unverifiable as written (M5) |
| ✅ frontend-mvp: Forward-compatible data access layer | Covered by static test + ESLint (tasks 2.16–2.17) |
| ⚠️ frontend-mvp: Transactions listing screen | "Ciclo actual" mismatch (M1); page size + total exposure unspecified (m5) |
| ⚠️ frontend-mvp: Category change modal | Contradicts data-policies "no re-fire on idempotent confirm" (M10) |
| ✅ frontend-mvp: Categories display in Spanish | Covered by fallback scenario (m6 noise) |
| ✅ frontend-mvp: Vercel deployment | Covered by 3.13–3.17 (3.17 vague — m4) |
| ✅ frontend-mvp: Frontend never aggregates money | Covered by negative scenario (m3 enforcement gap) |
| ✅ frontend-mvp: Documented agent rules | Covered by 2.18 |
| ❌ myfinance-data-policies: RLS on user-owned tables | Test coverage incomplete (M6: only 3 of 9 tables); existing V002 ignored (B3) |
| ❌ myfinance-data-policies: Read-only RLS on shared catalogs | Contradicts existing V002 (B3, Q4) |
| ❌ myfinance-data-policies: Feedback loop trigger | Table doesn't exist (B1, B2); SECURITY INVOKER breaks n8n path (B4); semantics drift vs SPEC.md §7 (B5) |
| ❌ myfinance-data-policies: Documented raw_pattern normalization | Algorithm unknown (M4); test fixtures unverifiable |
| ⚠️ myfinance-data-policies: Versioned migrations | Naming OK; idempotency claim fragile due to existing V002 collision (B3) |

---

### Scope assessment

**Scope creep:**
- None significant. The 65 tasks are scoped to MVP. Tasks 3.14–3.16 (Vercel deploy via MCP) are appropriate for the goal.
- Task 1.4 includes RLS on `exchange_rates` even though `proposal.md:13` says "vacías hoy; aplicar RLS por completitud". This is defensible defense-in-depth but it's also re-doing V002.

**Missing tasks:**
- No task creates `merchants` table (B1).
- No task adds `category_confirmed`, `merchant_id`, `ai_suggested_category_id`, `categorization_confidence` columns to `transactions` (B2; V008/TASK-DB-05).
- No task to backfill `display_name` (TASK-DB-01); the spec scenario "Display normal" assumes Spanish strings exist on every category.
- No task documenting Threat Model section (B7).
- No task to gate on `supabase-backup-policy` archival or to run pre-op snapshot before applying migrations to production (M2).
- No CI gate task (m9).
- No task to set Supabase JWT TTL to a reasonable value (M9).

---

### What I tried to break but couldn't

- Cross-user transaction read: D5 spec scenarios + V002 + JWT-driven `auth.uid()` correctly isolate transactions per user, assuming Testcontainers RLS test infra exists. The RLS model itself is sound where it's specified.
- Frontend money aggregation: explicitly forbidden, called out in proposal + spec + AGENTS.md task. Good belt-and-suspenders.
- Service layer leak: ESLint + static test combo is defensible (m3 caveat aside).

---

### Verdict justification

**7 Blockers + 10 Majors + 10 Minors + 5 Questions = FAIL.**

The change should NOT proceed to /opsx:apply in its current state. The seven Blockers fall into three buckets that each independently kill the implementation:

1. **Schema dependency cliff (B1, B2):** the trigger requires tables/columns that don't exist yet and are not created by this change. Branch #1 will fail at migration time. Either fold the pending V007/V008 migrations into this change, or sequence this change AFTER they land.

2. **Existing V002 ignored (B3, Q4):** the design.md authors did not read `backend/database/migrations/V002__rls_policies.sql`, which already enables RLS and creates the exact policies they propose. Branch #1's RLS migration will fail with policy-already-exists errors. The right answer is a small delta migration for the merchants table only, not a fresh policy set.

3. **Trigger semantics are not coherent (B4, B5, M8, M10):** SECURITY INVOKER + RLS + n8n/service_role + UPSERT + idempotency + the future Java backend doing the same feedback loop = multiple ways for the trigger to either fail silently (n8n updates), double-count (when backend Java lands), or contradict itself (re-confirm scenario). This is a Gate-A architectural decision that was skipped.

Additionally, B7 (missing Threat Model) is a hard workflow-gate prerequisite per `docs/workflow.md §2 Gate C`.

The 10 Majors include M1 (cycle window business logic risk) and M6 (RLS test coverage gap) that would cause the MVP to ship visibly broken or silently insecure.

**Cost-of-fix at this stage:** every Blocker and Major above is text-only changes in proposal.md / design.md / specs / tasks.md. The user is right that catching this now is 1/10 the cost of catching it during implementation. The fixes are mostly: (a) fold dependent migrations in, (b) reckon with V002, (c) decide the trigger ownership story, (d) add a Threat Model section, (e) tighten contradictions between the two specs.

**Recommended next steps:**

1. Add `## Threat model` to proposal.md (10 min, unblocks Gate C path).
2. Read `backend/database/migrations/V002__rls_policies.sql` and rewrite the `myfinance-data-policies` spec to be a DELTA on top of V002, not a replacement (1–2 h).
3. Decide via Gate A: does this change OWN the V007 (merchants) and V008 (transaction categorization fields) migrations, or block on them? Pick one (operator decision, 15 min).
4. Decide via Gate A: trigger ownership — DB-canonical forever (amend SPEC.md §7), or MVP-only with explicit removal task in TASK-BE-06, or skip the trigger entirely for the MVP (operator decision, 30 min).
5. Reconcile the re-confirm idempotency contradiction between the two specs (M10) (10 min).
6. Either include the n8n raw_pattern algorithm in the change scope (manual prereq for operator) or freeze on `raw_pattern = description` for MVP (M4) (operator decision).
7. Re-run adversarial review on the corrected proposal.

If the operator wants a path-of-least-resistance MVP TONIGHT: drop the trigger entirely from this change, drop the `myfinance-data-policies` capability, ship branch #2 + branch #3 only (frontend reads + writes via RLS that V002 already provides), accept that UI category changes do NOT update merchants until backend Java ships. This collapses 7 Blockers to ~1 Minor and the change becomes deployable in hours, not days.
