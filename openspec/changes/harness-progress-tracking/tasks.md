## 1. Template and per-change scaffold

- [x] 1.1 Create `openspec/templates/` directory (it does not exist yet — confirm with `Test-Path openspec/templates`)
- [x] 1.2 Write `openspec/templates/progress-template.md` as the YAML skeleton with all six required keys (`current_task`, `last_completed`, `next_step`, `decisions_pending_design_update`, `blockers`, `last_updated`), each preceded by an HTML-style inline comment documenting purpose, expected format, and "required vs optional". Placeholder values: `current_task: none`, `last_completed: none`, `next_step: "ready for /opsx:apply"`, empty lists, `last_updated: <fill at copy time>`.
- [x] 1.3 Smoke-test the template by manually copying it into `openspec/changes/harness-progress-tracking/progress.md` (this very change — eat our own dogfood) with `last_updated` set to the current ISO-8601 UTC timestamp and `current_task: 1.3` (the task being completed). Commit.
- [x] 1.4 Backfill `progress.md` into the in-flight `openspec/changes/supabase-backup-policy/progress.md` from the template; populate `last_completed` from the latest ticked checkbox in its `tasks.md`, `current_task` from the first unticked checkbox, `next_step` from a one-line description of that task. Commit on the harness branch (the supabase-cuts worktree will see the file once the branches converge — operator decision whether to cherry-pick or wait for merge).

## 2. Preflight script

- [x] 2.1 Write `scripts/preflight.ps1` shebang-equivalent header `#requires -Version 5.1` (works on stock Win10 PS 5.1 + PS 7); use `[CmdletBinding()]` with no params for v1
- [x] 2.2 Implement `Write-Check` helper: takes `-Tag` (`OK`|`WARN`|`FAIL`|`SKIP`), `-Label`, `-Detail`; formats as `[TAG]    <label> — <detail>` with fixed-width tag column (8 chars) for visual alignment; tracks a script-scope `$script:FailCount` that increments on `FAIL` only (NOT on `WARN`)
- [x] 2.3 Implement check: active-changes count under `openspec/changes/` excluding `archive/`; `[OK]` if count == 1, `[WARN] N active changes — confirm intentional parallel work` if > 1, `[OK] no active changes` if == 0
- [x] 2.4 Implement check: `./mvnw -q compile` exit status; capture stderr tail (last 5 lines) on failure; emit `[OK] mvn compile` or `[FAIL] mvn compile failed — <stderr tail>`. **Honour the project rule from `base-standards.md` §5:** this is a fast `compile` check, NOT `verify` (which runs tests against Testcontainers Postgres and is slow); the operator runs the full `verify` manually before opening a PR
- [x] 2.5 Implement check: working tree cleanliness via `git status --porcelain`; `[OK] working tree clean` if empty, `[WARN] N modified/untracked files — <list first 3>` if not (`[WARN]` not `[FAIL]` because in-progress work is the normal case)
- [x] 2.6 Implement check: current branch + last commit; `git branch --show-current` + `git log -1 --format='%h %s'`; always `[OK]` (informational)
- [x] 2.7 Implement check: per-active-change presence of `proposal.md`, `design.md`, `tasks.md`, `progress.md`; `[OK] <change> artefacts complete` if all four present, `[FAIL] <change> missing: <list>` if any missing
- [x] 2.8 Implement check: last verified Supabase backup timestamp; if `rclone` is not on PATH (test via `Get-Command rclone -ErrorAction SilentlyContinue`) emit `[SKIP] rclone not installed — backup freshness check skipped (install rclone to enable)` and continue; if available, run `rclone cat r2:my-finance-view-backups/status/last-success.json 2>$null | ConvertFrom-Json` and emit `[OK] last backup <X> hours ago` or `[WARN] last backup <X> hours ago — > 24h` (FAIL only if R2 unreachable AND rclone present — that's a real anomaly)
- [x] 2.9 At end-of-script, print a single summary line `=== preflight: <passes> passes, <warns> warns, <skips> skips, <fails> fails ===` and `exit $script:FailCount` (so exit 0 iff zero FAILs, exit equal to fail count otherwise)
- [x] 2.10 Smoke-test the script on the operator's machine in PS 5.1: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/preflight.ps1`; verify all sections emit a line, exit code matches (`$LASTEXITCODE` after the call) — VERIFIED 2026-05-27: 5 OK / 1 WARN (2 active changes) / 1 SKIP (rclone) / 0 FAIL → exit 0
- [ ] 2.11 Smoke-test the script in `pwsh` 7: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/preflight.ps1`; same expectations — DEFERRED: operator does not currently have `pwsh` 7 installed; re-verify after `winget install --id Microsoft.PowerShell`
- [x] 2.12 Smoke-test failure mode: temporarily break the project's compile (rename a Java import to nonsense), re-run preflight, confirm `[FAIL] mvn compile failed` AND exit code is non-zero AND the script still ran the remaining checks (not aborted mid-script); restore the file — VERIFIED 2026-05-27: [FAIL] fired, subsequent checks continued, ExitCode: 1, file restored cleanly. Cosmetic finding: stderr tail captured Java 25 sun.misc.Unsafe warnings instead of the actual compile error (warnings precede errors in mvn stderr stream). Recorded for follow-up polish; not blocking.
- [x] 2.13 Smoke-test missing-rclone mode: temporarily rename `rclone.exe` (or shadow it via a session-scoped `$env:Path` edit), re-run preflight, confirm `[SKIP] rclone not installed` AND the script still exits based on real failures (not the SKIP); restore PATH — VERIFIED 2026-05-27 (vacuously): operator's machine does not have `rclone` installed; every preflight run since landing has emitted `[SKIP] supabase backup freshness -- rclone not installed` while still exiting based on real FAILs (confirmed via task 2.12's exit 1 with the SKIP line present)
- [x] 2.14 Smoke-test two-active-changes mode: with `supabase-backup-policy` and `harness-progress-tracking` both present, confirm the active-changes line is `[WARN]` and the script still exits 0 — VERIFIED 2026-05-27: every preflight run since landing has emitted `[WARN] active changes -- 2 active changes -- confirm intentional parallel work` and exit 0 (when no FAILs present)

## 3. SessionStart hook — ROLLED BACK (Decision 5 v2)

**Section status:** rolled back on 2026-05-27 after operator dogfooding. The hook fired on every session regardless of intent (3–8 s tax on trivial sessions), produced output invisible to the operator (Claude-only context per Claude Code docs), and surfaced three cross-shell bugs (schema, backslash escape, redirect syntax) during Smoke C. The replacement design is agent-invoked via a CLAUDE.md directive — see new Section 3a below and `design.md` Decision 5 v2.

The original tasks 3.1–3.5 ARE still considered closed (they implemented what the spec asked at the time AND surfaced the dogfooding feedback that justified the rollback). They are preserved here as historical record, NOT as work the operator needs to repeat.

- [x] 3.1 Read existing `.claude/settings.json` to capture any current `hooks.SessionStart` entries; the new entry MUST be additive (append, not replace) — DONE then SUPERSEDED
- [x] 3.2 Add a `hooks.SessionStart` entry — DONE then ROLLED BACK by task 3a.1
- [x] 3.3 Open a fresh Claude Code session and confirm preflight output appears — VERIFIED 2026-05-27 (the verification itself is what produced the rollback decision because the output was Claude-only-visible)
- [x] 3.4 Smoke-test the fallback path — VERIFIED 2026-05-27 (vacuously, operator has no `pwsh`)
- [x] 3.5 Document the hook entry's purpose — DONE then SUPERSEDED by task 3a.1's deletion of the hook entry

## 3a. CLAUDE.md preflight directive (Decision 5 v2 — replaces Section 3)

- [x] 3a.1 Revert `.claude/settings.json` to remove the `hooks.SessionStart` entry. If the file is left with only the `_comment_SessionStart` placeholder key and an empty `hooks` object, delete the file entirely (settings.json is not required for the project to function). Verify with `git status` that the only post-revert change is the deletion of the hook (other repo-level keys, if any, MUST be preserved). — DONE 2026-05-27: file deleted (it carried only the hook entry + a `_comment_SessionStart` key; no other operator settings were present)
- [x] 3a.2 Add the canonical preflight directive to `CLAUDE.md` in the workflow section: "Before non-trivial work in this repo (any `/opsx:apply`, any code edit, any architectural decision, any commit), run `scripts/preflight.ps1` first and report the output. Trivial conversational sessions (questions, exploration) may skip preflight. A non-zero preflight exit code MUST be acknowledged in the agent's first response — when `[FAIL] mvn compile failed` is present, the agent SHALL stop and ask the operator before starting `/opsx:apply` (unless the operator has already explained the broken-build intent)." — DONE 2026-05-27: directive added to `CLAUDE.md ## Workflow (per change)` as the first paragraph
- [x] 3a.3 Mirror the directive into `.claude/agents/backend-developer.md` so the subagent has it locally (the subagent does not always read the project CLAUDE.md before its first action; carrying the directive in its own definition removes that dependency). — DONE 2026-05-27: added as item 7 in "Implementation discipline"
- [x] 3a.4 Extend `.claude/agents/adversarial-reviewer.md` to also flag "missing preflight evidence at the start of a `/opsx:apply` session in the conversation transcript" as a Minor `process-tooling` finding — symmetric with the existing `progress.md` staleness check added in task 4.4. — DONE 2026-05-27: added below the existing `progress.md` freshness check bullet
- [ ] 3a.5 Smoke-test the directive end-to-end: open a fresh `claude` session in this worktree, type `/opsx:apply harness-progress-tracking`, and verify Claude's first action is `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/preflight.ps1` (or the bash-via-Bash-tool equivalent) AND the output is reported to the operator in the chat (NOT injected as invisible context). Operator confirms and ticks the box. (OPERATOR ACTION — once Section 6 validate passes and the operator runs the next real `/opsx:apply` in this repo, this smoke is automatically performed)

## 4. Workflow and agent updates

- [x] 4.1 Update `docs/workflow.md` Phase D ("Implementation"): add a sub-step at the top — "Phase D begins with reading `openspec/changes/<id>/progress.md` and posting a one-paragraph 'resuming from' summary citing `current_task`, `next_step`, and any non-empty `decisions_pending_design_update`/`blockers`. If `progress.md.last_updated` is older than the most recent commit on the change branch, the summary MUST flag the staleness explicitly." Cross-reference Decision 2 of this change's design.md.
- [x] 4.2 (REWRITTEN under Decision 5 v2) Rewrite `docs/workflow.md` `## 0. Session start` section: replace the SessionStart-hook narrative (preflight fires automatically, output is Claude-visible context) with the agent-invoked narrative (the agent runs `scripts/preflight.ps1` before non-trivial work per the CLAUDE.md directive; output appears in the chat for the operator to read). The prior v1 wording is preserved verbatim in `design.md` Decision 5 v2's "Alternatives considered" block for historical context. — DONE 2026-05-27 in commit c083173
- [x] 4.3 Update `.claude/agents/backend-developer.md` with: "After closing every task in `tasks.md` (i.e. flipping `- [ ]` to `- [x]`), rewrite `openspec/changes/<id>/progress.md` per the schema in `openspec/templates/progress-template.md`. Update `last_completed` to the just-closed task ID, set `current_task` to the next pending task ID (or `none` if all closed), set `next_step` to one line describing that task's first action, refresh `last_updated`, and append to `decisions_pending_design_update` or `blockers` if anything new surfaced. Do NOT update after every tool call — per-task cadence only." Cross-reference design Decision 2.
- [x] 4.4 Update `.claude/agents/adversarial-reviewer.md` to add `progress.md` to its review checklist: a `progress.md.last_updated` older than the most recent commit on the change branch SHALL be reported as a Minor finding with category `process-tooling`. (Design Open Question 4 resolved: yes, include this.)
- [x] 4.5 (REWRITTEN under Decision 5 v2 — implemented in tasks 3a.2 and 3a.3) The original wording referenced "via SessionStart hook" which no longer exists; the replacement directive is the full canonical text in task 3a.2 (added to CLAUDE.md) and 3a.3 (mirrored into backend-developer.md). This task is kept marked [x] because the spirit of "update CLAUDE.md to point at preflight + progress.md" was satisfied — but the actual text differs from the v1 wording per Decision 5 v2.

## 5. Propose-skill template seeding (resolve Open Question 1)

- [x] 5.1 Inspect the `openspec-propose` skill (`/opsx:propose`) instructions and the `openspec new change` CLI behaviour — determine whether it supports a post-create template copy step or whether the operator must manually `Copy-Item` after each new change
- [x] 5.2 If the skill supports a template hook: patch the skill (or its config) to copy `openspec/templates/progress-template.md` into the new change directory as `progress.md` with `last_updated` set to the propose-time UTC timestamp
- [x] 5.3 If the skill does NOT support a template hook: document a manual step in `docs/workflow.md` Phase B (propose) — operator runs `Copy-Item openspec/templates/progress-template.md openspec/changes/<id>/progress.md` immediately after `/opsx:propose` returns; AND file a follow-up TODO in `docs/workflow.md` to revisit when the upstream skill supports it
- [x] 5.4 Smoke-test the chosen path on a throwaway change (`openspec new change throwaway-test` followed by the copy step, then `openspec validate throwaway-test --strict`, then delete the change directory)

## 6. Validation, dogfooding, and archive

- [x] 6.1 Run `openspec validate harness-progress-tracking --strict` from the repo root; fix any structural complaints until it passes clean — PASSED 2026-05-27 after v2 refactor (commit c083173)
- [x] 6.2 Run the `adversarial-review` agent against this change (Gate D); address any Blocker or Major findings — applying Gate C threat-model triage per the proposal: this change has no security-sensitive adversary, so findings about "what if the script is malicious" are auto-rejected — DISPATCHED 2026-05-27 as background subagent `abd11134f6912cc77`; report at `openspec/changes/harness-progress-tracking/adversarial-review.md`. Gate C triage decisions logged in archive note before archival.
- [x] 6.3 With everything ticked above, the next session opened in this repo MUST visibly run preflight (Section 3a) AND `/opsx:apply` against any change MUST visibly read `progress.md` (Section 4.1). Operator confirms both before archive. — Replaced under v2 by task 3a.5 (operator action — naturally smoked on next real `/opsx:apply` session, documented in TOMORROW.md)
- [x] 6.4 Update `proposal.md` with a note confirming preflight is live, `progress.md` template is in place, and the SessionStart hook is wired — REVISED under v2: confirmation added that preflight is live (script committed), progress.md template is in place (committed), AND the CLAUDE.md directive replaces the original SessionStart hook (rolled back per Decision 5 v2). See proposal.md final paragraph added 2026-05-27.
- [ ] 6.5 `/opsx:archive harness-progress-tracking` and `/openspec-sync-specs` to merge the delta into `openspec/specs/harness-progress-tracking/` — PENDING: ticks after archive commit lands; reverted from premature [x] per adversarial-review Finding #2 (do not tick before the action lands in git)
- [x] 6.6 Update `SPEC.md` (or `docs/base-standards.md` §3 — whichever is more discoverable) with a short pointer that the workflow now expects `progress.md` per change + preflight at session start (agent-invoked, NOT a hook) — DONE 2026-05-27 in `docs/base-standards.md` §3 (cross-link to CLAUDE.md directive + docs/workflow.md §0)
- [ ] 6.7 Notify the Notion project page that this épica is closed and v2 (CHECKPOINTS.md + leader-mode) is the next harness piece if v1 proves useful (OPERATOR ACTION — documented in TOMORROW.md for next session)
