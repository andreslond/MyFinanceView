## Adversarial Review · supabase-backup-policy v1 cuts · 2026-06-02

**Verdict:** FAIL

**Scope reviewed:**
- 4 files in diff (proposal.md +9 lines / tasks.md +22 lines / design.md +43 lines / spec.md +56 lines, all ignore-cr-at-eol)
- Internal-coherence pass only — v1-cut decisions themselves are out of scope per spawner brief
- Cross-checked against: .env.example, progress.md, notes/operator-prereqs-checklist.md

### Findings

#### Blockers

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| B1 | Blocker | Env-var contract drift — implementation files contradict the v1 spec | `.env.example:79-89` still defines `MYFINANCE_BACKUP_GMAIL_APP_PASSWORD` (with Gmail SMTP comment) and `MYFINANCE_BACKUP_KUMA_PUSH_URL` (with Kuma comment), and contains NO `MYFINANCE_BACKUP_RESEND_API_KEY` / `MYFINANCE_BACKUP_ALERT_FROM` / `MYFINANCE_BACKUP_ALERT_TO` placeholders. spec.md scenario "No Kuma push URL is configured in v1" at spec.md:434-437 asserts exactly the opposite ("no `MYFINANCE_BACKUP_KUMA_PUSH_URL` placeholder is present in `.env.example`"). tasks.md §2.4 (now `[ ]` unchecked) prescribes the v1 contract, but the file in the working tree fails the spec scenario as written today. | code: rewrite `.env.example` lines 76-89 to drop Gmail+Kuma blocks and add Resend trio (`MYFINANCE_BACKUP_RESEND_API_KEY`, `MYFINANCE_BACKUP_ALERT_FROM`, `MYFINANCE_BACKUP_ALERT_TO`); include this edit in the same commit as the spec edits so the scenario doesn't fail at archive-time validation. |
| B2 | Blocker | Workflow file-count drift — design.md still claims FIVE | `design.md:177` (in the line headed "trade-off"): "This is the **fifth** workflow file in `scripts/backup/n8n/` (counting the PreOp manual-trigger workflow)." — but this line is the older v3 sentence that was NOT updated by the diff. Other passages in the SAME file now claim FOUR (design.md:171 "fourth workflow file… Watchdog dropped in v1"). The contradiction is in design.md itself. **Confirm**: grep on the working-tree file shows the sentence at design.md ~line 177 still says "fifth". | spec: rewrite design.md ~line 177 area to say "fourth" and drop the Watchdog enumeration; align with design.md:171 and with proposal.md "four n8n workflow JSONs". |
| B3 | Blocker | spec.md Repository-layout Requirement still mandates Kuma push as worker behavior | `spec.md:341`: "`workers/backup-daily.sh` — bash worker invoked by `POST /run/daily`. Performs `pg_dump`, tar, age (single recipient), rclone upload, **post-upload SHA-256 re-verify**, weekly/monthly server-side promotion, **chained restore-verify (reviewer Q2)**, quarantine-routing on failure, `status/last-success.json` upsert, and Uptime Kuma push." This Requirement contradicts spec.md:434-437 (Scenario asserting NO Kuma push branch in `backup-daily.sh`) and tasks.md §4.3.12 (Kuma push dropped). A spec Requirement that contradicts another spec Scenario in the same delta is a hard internal-coherence failure. | spec: edit spec.md:341 to drop "and Uptime Kuma push"; add the v1-cut callout used elsewhere. |
| B4 | Blocker | spec.md HTTP API description still cites Kuma push inside `/run/daily` | `spec.md:388`: "`POST /run/daily` — triggers the daily backup pipeline (… → chained restore-verify → status JSON upsert). v1 (2026-06-01): no Kuma success push (Kuma layer dropped)." — **this one is consistent**. BUT `design.md:386` (HTTP API contract table, Notes column): "Synchronous. Sidecar handles weekly/monthly promotion + Kuma push internally." remains unchanged. Design and spec disagree at the same surface. | spec: edit design.md:386 Notes column to drop "+ Kuma push internally" so the HTTP API table matches spec.md:388. |
| B5 | Blocker | design.md HTTP API table still says `/status` is used by `MyFinanceBackup-Watchdog` | `design.md:385` (`/status` row, Notes column): "Read from `/var/lib/myfinance-backup/status/*.json`. Used by `MyFinanceBackup-Watchdog`." — Watchdog is dropped per spec.md §"No Watchdog workflow is committed in v1" (spec.md:439-442) and per tasks.md §7.4. The HTTP API table is the authoritative interface contract for the sidecar and now references a workflow that does not exist. | spec: edit design.md:385 Notes column — replace "Used by `MyFinanceBackup-Watchdog`" with "exposed for ad-hoc operator inspection; no scheduled in-cluster consumer in v1 (Watchdog dropped — see Decision 7)". |

#### Majors

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| M1 | Major | spec.md "Backup coverage" Requirement still says watchdog performs an RLS-policy check | `spec.md:316`: "`pg_dump -n myfinance -Fc` DOES capture row-level security policies … **The watchdog SHALL include a one-time documented check during the initial smoke-test** that confirms restored RLS policies match the production count …" — the watchdog is dropped; this SHALL is now stranded. The intent is fine (one-time bootstrap check during smoke test) but the subject ("the watchdog") no longer exists. | spec: rewrite spec.md:316 to attribute the check to `tasks.md §9.x` smoke test (operator runs the count manually during initial bootstrap), not to a non-existent watchdog. |
| M2 | Major | design.md R2 op-count math still budgets Class B reads for "watchdog ticks" | `design.md:114`: "status JSON reads via the `/status` endpoint (worst-case 4 reads × every watchdog tick = 28 reads/week assuming daily watchdog ≈ 4 reads/day) + the watchdog's `lastDrill` read = 5 reads/day." — the watchdog is gone; this budget line is mathematically OK (headroom still vast) but the math is now reasoning about a workflow that does not exist. A future reviewer trying to validate the op-count under v1 will be confused. | spec: rewrite design.md:114 Class B paragraph — drop watchdog enumeration; budget only what v1 actually does (ad-hoc operator `/status` peek + the chained-verify `GetObject`). |
| M3 | Major | tasks.md §2.5.5 key-rotation list still names Gmail App Password + Kuma URL | tasks.md:25: "Key rotation — age recipient (rebuild image after replacing the file under `recipients/`), age identity (re-encrypt drill), R2 token, **Gmail App Password, Kuma push URL**, runner shared-secret". This is the operator-facing README contract for what gets rotated. Both items dropped in v1. | code/spec: rewrite tasks.md:25 to enumerate the v1 credentials — Resend API key, ntfy topic, runner shared-secret, R2 token, age identity. Note: this task is marked `[x]` (i.e. already-committed README content); flipping the task back to `[ ]` is appropriate since README content must change. |
| M4 | Major | progress.md is stale and now factually wrong about the v1 contract | `progress.md:11` `next_step`: "Operator §1 prereqs — install age on Windows PC, generate single primary identity, configure R2 token + **5 lifecycle policies** + **Gmail App Password** + ntfy topic + **Kuma push URL** + Supabase role password + runner shared secret". Three independent v1 cuts are violated by this single line. `progress.md:32` also still references "Daily/PreOp/Watchdog" enumeration for M3 fix wording. `last_updated: "2026-06-01T00:00:00Z"` is older than the in-flight edits to spec/design/tasks/proposal on 2026-06-01–02. Per CLAUDE.md harness directive (`harness-progress-tracking` Decision 2), the per-change progress file is the source of truth for "where is this change RIGHT NOW" — a stale file misleads the next session. | process-tooling: rewrite progress.md `next_step` to enumerate v1 prereqs (1 lifecycle, Resend API key + alert addresses, ntfy, runner secret); update `last_updated` to a 2026-06-02 timestamp; remove Watchdog enumeration from line 32. |
| M5 | Major | notes/operator-prereqs-checklist.md still drives the operator through Gmail + Kuma steps | `notes/operator-prereqs-checklist.md:20` and `:21` capture-table rows for `MYFINANCE_BACKUP_GMAIL_APP_PASSWORD` (Paso 6) and `MYFINANCE_BACKUP_KUMA_PUSH_URL` (Paso 9). `:112-118` is an entire "Paso 6 — Gmail App Password" section. `:153-164` is an entire "Paso 9 — Uptime Kuma Push Monitor" section including Push-URL capture and notification-channel wiring. `:171` then tells the operator to wire Gmail SMTP into Kuma. This is the live checklist `progress.md` literally calls "next_step" — the operator is about to follow this document **today** per the spawner brief, and it will lead them to provision Gmail+Kuma despite the spec dropping both. | spec/docs: rewrite `notes/operator-prereqs-checklist.md` capture table and Paso 6 + Paso 9 — replace with Paso 6 = Resend API key generation on `datachefnow.com`, drop Paso 9 entirely (or replace with a "skip — Kuma deferred in v1" callout). Update Resumen final at the bottom of the file. |
| M6 | Major | spec.md §"Quarantine retention is 365 days" Scenario asserts a deleted lifecycle rule | `spec.md:222-225` Scenario "Quarantine retention is 365 days": "**WHEN** the operator inspects the bucket lifecycle configuration for the `quarantine/` prefix / **THEN** the configured day count is 365 — long enough for forensic inspection without growing the bucket unbounded". Under v1 (spec.md:131-137 new lifecycle Requirement) ONLY `daily/` 30d exists; the `quarantine/` 365d rule is explicitly deferred. This Scenario will fail at archive-time validation OR will lead the operator to configure a rule the new Requirement says NOT to configure. | spec: delete spec.md:222-225 Scenario (or rewrite it as "v1 accepted gap — quarantine has no lifecycle rule; see Decision 3"). |

#### Minors

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| m1 | Minor | proposal.md "Infrastructure carve-out" still includes Uptime Kuma as an exemplar | `proposal.md:56`: "infrastructure services (n8n, this sidecar, Traefik, **Uptime Kuma**) live as separate processes under `scripts/<infra-domain>/`". Kuma is dropped in v1 — naming it as the canonical second infrastructure-tier exemplar in the SPEC.md amendment text is misleading. | spec: edit proposal.md:56 — drop Uptime Kuma from the parenthetical, or reword to "(n8n, this sidecar, Traefik, future infra additions)". |
| m2 | Minor | design.md "Resolved Questions" deduplicate problem | `design.md:464-471` adds three new RESOLVED entries (Resend, R2 lifecycle, IP allowlist) but also preserves the prior "Reason slug regex" entry duplicated — minor: the diff shows the same Reason-slug line both deleted and re-added, which is just noise from CRLF re-encoding rather than a content change. Risk is low but the diff is harder to audit. | process: re-run the edit with `git diff --ignore-cr-at-eol HEAD -- design.md` confirmed clean before committing. |
| m3 | Minor | Migration plan numbering kept the Kuma-shaped hole as a "DROPPED" step | `design.md:439`: "4. **Uptime Kuma (DROPPED in v1, operator decision 2026-06-01)** — no Push Monitor is created. … Skip this step." The numbering 1→2→3→4(dropped)→5→6→7→8→9→10→11→12→13 is coherent but step 4 is a no-op the operator is told to skip. Mildly confusing for a one-shot bootstrap doc; could be collapsed without changing meaning. | spec: optional — renumber 4→5→…→12 to remove the no-op step. Acceptable to leave as-is for diff readability. |
| m4 | Minor | tasks.md §9.7 dropped the assertion the operator should ALSO see "no spurious alert" on success | tasks.md:196 §9.7 (success-path smoke): previously asserted "the Kuma monitor shows 'Up'", now simply asserts `last-success.json` + `last-verify.json` update green. The Resend equivalent positive-confirmation ("operator confirms NO email arrived on success path") is not stated. Minor: success-path = silence is the documented behavior (spec.md:248-251 "Successful run does not alert"), but smoke §9.7 doesn't probe it. | tests: add "AND confirm no Resend email arrived in the operator inbox within 5 min of the success run" to tasks.md:196. |
| m5 | Minor | proposal.md "Risks accepted in design" still bolts a webhook claim onto the no-webhook design | `proposal.md:60` (the long final-bullet "Risks accepted in design" rewrite) includes "Pre-op webhook public surface eliminated (v1, operator decision 2026-06-01): no Traefik IP allowlist; the 238-bit `MYFINANCE_BACKUP_RUNNER_SECRET` bearer …" — but the public webhook was already dropped in v3 (Decision 9 per design.md). This is the spawner's third v1 cut and is internally fine, BUT the bullet reads as if v1 newly eliminated the webhook, conflating two separate decisions. Risk: future reader thinks the v3 cut was incomplete. | spec: edit proposal.md:60 to clarify "v3 already dropped the public webhook; v1 confirms the internal sidecar bearer is the sole guard with no Traefik IP allowlist on top, accepted because…". |

#### Questions

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| Q1 | Question | Is the `proposal.md` "VPS process failure" threat-model bullet honest about the new uncovered case? | `proposal.md:13` rewrite: "**VPS process failure.** n8n or the sidecar silently dies … silent Schedule-Trigger non-fire (VPS up, n8n cron didn't tick) is now an uncovered failure mode mitigated only by adjacent symptoms the external monitor sees (e.g. n8n process crash)." But the threat-model section is in the "what this change defends against" preamble — listing a defended-against threat that the system actually does NOT detect in one common sub-mode is unusual. Per Gate A semantics the threat is still "in model" because some sub-modes are defended (in-flight failures via ErrorHandler), but the prose admits the silent-non-fire is uncovered. | clarify: is the silent-non-fire (a) explicitly accepted-out-of-model with a Gate-C-style "auto-reject" callout in the same paragraph, or (b) in-model but admittedly imperfectly defended in v1? Current wording is (b); confirm operator intent and tighten the prose accordingly. |
| Q2 | Question | spec.md §"R2 transient outage during status upsert is fatal" Scenario references Kuma in rationale prose | `spec.md:46` (Scenario rationale, end of the long THEN sentence): "**v1 (operator decision 2026-06-01):** the in-cluster Watchdog + Kuma layer that previously also caught this case via stale-detection was removed; the explicit ntfy + Resend dispatch is now the only signal for a green-verify-but-stale-status failure." This is honest and v1-aware, but it leaves the *prior* paragraph (the THEN clause itself) reading "log the failure, dispatch the ntfy + Resend alert, AND exit non-zero" — fine — followed by a Kuma-historical rationale. Is the long "Rationale" sentence intended to remain in the spec for posterity, or should it be pruned? | clarify: trim to single-paragraph rationale focused on v1 behavior; move v3-comparison prose to design.md "Alternatives considered". |

### Acceptance-criteria coverage

- ✅ Proposal.md / tasks.md / design.md text bodies agree on "two channels" (ntfy + Resend), "four workflows" (no Watchdog), "one lifecycle rule" — but inline references inside design.md (line 177 "fifth"; lines 385-386 "Watchdog" / "Kuma push") and spec.md (line 341 "Uptime Kuma push") contradict them. See B2–B5.
- ❌ `.env.example` contract documented in tasks.md §2.4 does NOT match the actual `.env.example` in the working tree. See B1.
- ❌ spec.md §"Backup coverage" still references "the watchdog" as the executor of an RLS check. See M1.
- ❌ Scenario "Quarantine retention is 365 days" (spec.md:222-225) asserts a lifecycle rule that v1 explicitly defers. See M6.
- ✅ Disaster-drill cadence: spec.md Scenario rewritten to "calendar-only" (spec.md:302-305) consistent with tasks.md §10.6 wording; coherent.
- ✅ Migration plan numbering: gap-filled with explicit "DROPPED" step 4 — readable but see m3.

### Scope-creep / missing assessment

- No scope creep in the four edited files relative to the v1 cuts as briefed.
- Missing edits (mandatory to ship): `.env.example` (B1), `progress.md` (M4), `notes/operator-prereqs-checklist.md` (M5). These three files are essential supporting artefacts of the change and remain in their v3 state.

### What I tried to break but couldn't

- **Two-channel parallel fan-out semantics:** the rewritten `MyFinanceBackup-DispatchAlert` (tasks.md §7.6) preserves the `Continue On Fail = true` pattern on both legs and the never-returns-non-2xx contract, so the v3 "ntfy outage does not block email" Scenario (now Resend, spec.md:253-256) still holds end-to-end.
- **Reason-slug regex retained:** the v1 cuts did NOT touch the regex defence-in-depth (tasks.md §4.4.1 + §7.2 + Decision 9). Pre-op injection via filename is still blocked.
- **Idempotency / same-day re-run:** untouched by the v1 cuts; the "Same-day re-run of daily workflow is idempotent" Scenario remains valid.
- **age stdin handling B7 fix:** untouched and still consistent across design.md + tasks.md + spec.md.

### Process-tooling findings (per agent directive)

- **progress.md freshness (Minor → escalated to Major):** see M4. `last_updated: 2026-06-01T00:00:00Z` is older than the in-flight 2026-06-01/02 edits; the `next_step` actively misdirects the operator's next 30 minutes. Escalated above the normal Minor threshold because the operator is about to act on it today per the spawner brief. Recommended fix: rewrite `progress.md > next_step` to reflect the v1 prereqs (Resend, single lifecycle, no Kuma), and bump `last_updated` to a 2026-06-02 timestamp.
- **Preflight evidence at /opsx:apply start:** N/A for this session (this is a spec-edit session, not /opsx:apply implementation work; transcript not in scope for the reviewer).

### Verdict justification

5 Blockers (B1–B5: env-var contract drift, four file-internal contradictions in spec.md + design.md authoritative tables/Requirements) + 6 Majors (M1–M6: stranded Requirement subject, stale op-count math, stale rotation list, stale progress.md, stale operator checklist, stale quarantine retention scenario) → FAIL.

The v1-cut decisions themselves are coherent and well-rationalized in the operator-facing prose. The failure mode is that the cuts were applied to the narrative passages (proposal preface, design Decisions, spec Requirement headings) but NOT propagated to the structural artefacts that downstream tooling and the operator actually read at execution time:

1. The `.env.example` file the operator copies to `.env.local`.
2. The HTTP API contract table in design.md.
3. The Repository-layout Requirement in spec.md.
4. The Backup-coverage Requirement in spec.md.
5. The progress.md `next_step` the next session will resume from.
6. The operator-prereqs-checklist.md the operator is about to execute.

Resolve all 5 Blockers + M1, M3, M4, M5, M6 before `/opsx:archive`. M2 and the Minors can be batched into the same commit or deferred with explicit follow-up notes.

VERDICT: FAIL
Counts: 5 Blocker / 6 Major / 5 Minor / 2 Question
