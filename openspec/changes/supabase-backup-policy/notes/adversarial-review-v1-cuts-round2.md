## Adversarial Review · supabase-backup-policy v1 cuts · Round 2 · 2026-06-02

**Verdict:** PASS WITH GAPS

**Scope reviewed:**
- HEAD = `97355e7 feat(backup): apply v1 scope cuts — lifecycle, alerts (Resend), no Traefik allowlist`
- 8 files changed by pass-2 commit (`.env.example`, `proposal.md`, `design.md`, `tasks.md`, `specs/database-backups/spec.md`, `progress.md`, `notes/operator-prereqs-checklist.md`, plus the Round-1 review note itself for cross-reference).
- Re-verified each Round-1 Blocker (B1–B5) and Major (M1–M6) against current file:line.
- Also grepped the broader repo (`scripts/backup/`) for leftover Gmail/Kuma/Watchdog mentions.

### Round-1 Blockers verification

| # | Status | Evidence at HEAD |
|---|---|---|
| B1 | **FIXED** | `.env.example:76-97` rewrites the Alerting block: drops `MYFINANCE_BACKUP_GMAIL_APP_PASSWORD` + `MYFINANCE_BACKUP_KUMA_PUSH_URL`, adds `MYFINANCE_BACKUP_RESEND_API_KEY`/`MYFINANCE_BACKUP_ALERT_FROM`/`MYFINANCE_BACKUP_ALERT_TO`. The "v1 cuts" comment block at 93-97 enumerates the dropped vars with reinstate-by-readding instruction. spec.md:434-437 scenario "No Kuma push URL is configured in v1" is now satisfied by the working tree. |
| B2 | **FIXED** | `design.md:171` (DispatchAlert bullet): "This is the **fourth and final** workflow file in `scripts/backup/n8n/` for v1 (Daily, PreOp manual-trigger, ErrorHandler, DispatchAlert — Watchdog dropped together with Uptime Kuma per Decision 7)." The prior "fifth" sentence is gone. Daily bullet at design.md:167 and Watchdog bullet at design.md:169 explicitly say "Watchdog dropped in v1". Repo-wide grep for `fifth|five workflow|FIVE` returns no hit in design.md / proposal.md / tasks.md / spec.md (only matches the Round-1 review note itself). |
| B3 | **FIXED** | `spec.md:341` now reads: "… `status/last-success.json` upsert. **v1 (operator decision 2026-06-01):** no Uptime Kuma success push (Kuma layer dropped; see proposal.md ## Threat model and design.md Decision 7)." No "Uptime Kuma push" in worker behavior any more. |
| B4 | **FIXED** | `design.md:386` `/run/daily` Notes column: "Synchronous. Sidecar handles weekly/monthly promotion internally. **v1 (2026-06-01):** no Kuma success push (Decision 7)." Aligns with spec.md:388 wording. |
| B5 | **FIXED** | `design.md:385` `/status` Notes column: "… **v1 (2026-06-01):** exposed for ad-hoc operator inspection; no scheduled in-cluster consumer in v1 (Watchdog dropped — see Decision 7)." The dangling `MyFinanceBackup-Watchdog` reference is gone. |

### Round-1 Majors verification

| # | Status | Evidence at HEAD |
|---|---|---|
| M1 | **FIXED** | `spec.md:316`: "The operator SHALL perform a one-time documented check during the initial bootstrap smoke-test (`tasks.md` §9.x) … **v1 note (operator decision 2026-06-01):** the previous wording attributed this check to the Watchdog workflow, which has been dropped together with Uptime Kuma; the check moves to operator discipline executed during initial bootstrap." Subject of the SHALL is now the operator, not the dropped Watchdog. |
| M2 | **FIXED** | `design.md:114` Class B paragraph now reads: "occasional ad-hoc `/status` peek by the operator (a handful of `GetObject` on the status JSON files — not on a schedule in v1 since Watchdog was dropped)." Watchdog tick math removed; operator-eyeball wording substituted. |
| M3 | **FIXED** | `tasks.md:25` (§2.5.5 rotation list): "age recipient … R2 token, **Resend API key**, ntfy topic, runner shared-secret. **v1 cut (operator decision 2026-06-01):** Gmail App Password and Kuma push URL are no longer rotated (both replaced or deferred)." Task is also flipped to `[ ]` correctly because the README content change is now pending implementation. |
| M4 | **FIXED** | `progress.md:11` (`next_step`): "Operator §1 prereqs (v1 contract, 2026-06-01 cuts applied) — … configure R2 token + ONE `daily/` 30d lifecycle policy + Resend API key … + ntfy topic + Supabase role password + runner shared secret. **No** Gmail App Password (Gmail → Resend), **no** Uptime Kuma Push Monitor …". All three v1 cuts reflected. `progress.md:18` `last_updated: "2026-06-02T00:00:00Z"` is current. `decisions_pending_design_update` line at 16 records the pass-3 (i.e. this round's predecessor) FAIL verdict and the fixes; line 33's Watchdog reference has been replaced with the §8.4a note about Daily/PreOp re-linking (no Watchdog mention). |
| M5 | **FIXED** | `notes/operator-prereqs-checklist.md:20`/`:21` capture table now lists `MYFINANCE_BACKUP_RESEND_API_KEY` + `MYFINANCE_BACKUP_ALERT_FROM` + `MYFINANCE_BACKUP_ALERT_TO` (Paso 6). `:25` adds the explicit "v1 cuts" callout for the dropped Gmail/Kuma vars. `:113-135` is a fully rewritten "Paso 6 — Resend API key" section. `:170-176` is a "Paso 9 — Uptime Kuma Push Monitor (DROPPED v1)" callout that tells the operator "No hagas este paso". `:223-229` ("Estado en cuentas externas (v1)") cleanly enumerates only R2 / Resend / ntfy / Supabase, with the "No requerido en v1" line explicitly naming the dropped items. |
| M6 | **FIXED** | `spec.md:222-225` Scenario rewritten as "Quarantine has no lifecycle rule in v1 (accepted gap)": **WHEN** operator inspects bucket lifecycle for `quarantine/` → **THEN** NO lifecycle rule, with explicit "Accepted gap (operator decision 2026-06-01)" framing and pointer to the §10.5 spot-check. No more "365 days" assertion. |

### New findings (Round 2)

| # | Severity | Area | Evidence | Recommended fix |
|---|---|---|---|---|
| N1 | Major | Implementation drift — workers + n8n JSON still v3 (Gmail+Kuma) | `scripts/backup/workers/alert.sh:2,15,40-64` still implements Gmail SMTP via curl using `MYFINANCE_BACKUP_GMAIL_APP_PASSWORD`; `scripts/backup/workers/backup-daily.sh:229-239` still has the Kuma push branch on `MYFINANCE_BACKUP_KUMA_PUSH_URL`; `scripts/backup/n8n/MyFinanceBackup-DispatchAlert.json:55-118` still wires a "Gmail SMTP Alert" node with `fromEmail: aftorresl01@gmail.com`. **This IS in-scope for the v1 cuts** because tasks.md §4.2, §4.3.12, and §7.6 are correctly flipped to `[ ]` (acknowledged unfinished) — but the operator-prereqs-checklist (M5) now tells the operator NOT to provision Gmail App Password / Kuma URL, and a deploy of HEAD code with the v1 prereqs would silently degrade to ntfy-only on alert dispatch (alert.sh:71 returns 0 if at least one channel succeeds; Gmail branch logs "MYFINANCE_BACKUP_GMAIL_APP_PASSWORD not set — skipping Gmail alert"). Functionally graceful, but: (a) the operator gets a misleading log line every alert; (b) more importantly, **the Resend dispatch path is not implemented anywhere yet**, so the v1 "two-channel alerting" Requirement (spec.md:232–267) is unsatisfiable until §4.2/§7.6 land. | code: complete §4.2 (`alert.sh` Resend rewrite) and §7.6 (DispatchAlert JSON rewrite) before `/opsx:archive`. tracking: §4.2 + §4.3.12 + §7.6 are already unchecked in tasks.md so the implementer knows; this finding raises the priority — without §4.2 the Resend leg simply does not exist in the worker, and the spec Requirement at spec.md:232–267 has no implementation. |
| N2 | Major | Authoritative runbook drift — `scripts/backup/README.md` still describes Gmail+Kuma as the architecture | `scripts/backup/README.md:13-14`: "Alerts via ntfy.sh push + Gmail SMTP on any failure. / Monitors liveness via Uptime Kuma in-VPS dead-man-switch." `:51` ASCII-art shows "Kuma push ping" lane. `:196-202` key-rotation table has dedicated "Gmail App Password" and "Kuma push URL" rows + a "Notification channels wired to Kuma monitor" paragraph. README.md is the operator-facing runbook (installed at the same path as `scripts/backup/recipients/primary.txt`); it is the artefact tasks.md §2.5 enumerates. A first-time reader following the README on the VPS after a v1 deploy will be told the architecture includes Kuma and Gmail and will look for env vars / dashboards that the v1 cuts say not to provision. Tasks.md §2.5.5 (M3) is correctly unchecked, but §2.5.1/§2.5.2 (overview + architecture) are checked `[x]` — they should NOT be, because their content predates the v1 cuts. | code: rewrite `scripts/backup/README.md` Overview + Architecture + Key-rotation sections to match the v1 alerting model (ntfy + Resend, no Kuma); flip `tasks.md` §2.5.1/§2.5.2 back to `[ ]` since their content now needs revising. |
| N3 | Minor | `test-smoke.sh` exports the dead v1 env vars | `scripts/backup/test-smoke.sh:135-136` still does `export MYFINANCE_BACKUP_GMAIL_APP_PASSWORD=""` and `export MYFINANCE_BACKUP_KUMA_PUSH_URL=""`. Harmless (both are empty), but a future maintainer reading test-smoke would think these are part of the v1 contract. | code: remove lines 135-136; add `export MYFINANCE_BACKUP_RESEND_API_KEY=""` (and `ALERT_FROM`/`ALERT_TO`) once §4.2 lands. |
| N4 | Minor | progress.md `decisions_pending_design_update:16` calls the just-finished review "pass 3" but its predecessor at line 15 calls itself "pass 2" — numbering coherent inside this change. However, the round just performed (this report, after Round 1 of the v1-cuts review) becomes the FOURTH adversarial-review pass on the change overall (replant pass 1 + 2 + 3 documented at lines 14-15, plus this v1-cuts Round 1 + Round 2). Calling THIS the "pass 3" again (line 16 currently does) will conflict with the prior replant pass-3 entry at line 14. | process: when progress.md is next rewritten, rename "pass 3" at line 16 to "v1-cuts pass 1" (round-1 review) and add a "v1-cuts pass 2" line for this round once it lands. Cosmetic; does not block archive. |
| N5 | Minor | `notes/operator-prereqs-checklist.md:109` keeps a "Paso 5 — (skipped — ya hecho con paso 4)" placeholder rather than renumbering | The `Paso N` sequence has a no-op step 5 that the operator must visually skip. Same class as Round-1 m3 ("Migration plan numbering kept the Kuma-shaped hole"). Acceptable for diff readability; flagged for visibility. | process: optional — collapse to `Paso 4` → `Paso 5` = Resend → `Paso 6` = ntfy → `Paso 7` = Supabase → `Paso 8` = Kuma-dropped callout (or remove entirely) → `Paso 9` = runner secret. Defer if the operator prefers stable numbering. |
| N6 | Question | progress.md `current_task: "1.1"` / `last_completed: "7.7"` claims §1 is the next thing to do — but lines at progress.md:32-34 say things like "tasks.md 8.4a added" and "Final adversarial-review verdict (pass 3, 2026-06-01): PASS". The progress file conflates the replant pass-3 PASS (which was BEFORE the v1 cuts) with the v1-cuts pass-1 FAIL (which immediately followed) and this v1-cuts pass-2 review. Net effect: progress.md `next_step` is correctly pointing the operator at §1 (the right action), but the line-by-line `decisions_pending_design_update` log is hard to read because three separate adversarial reviews on different baselines are interleaved. | clarify: is the operator's intended next 30 min still "execute §1 from operator-prereqs-checklist.md", or do §4.2/§7.6/§2.5 implementation tasks need to land first (per N1+N2)? My read: §1 is operator-only manual prereqs (no code), so it can run in parallel; but §4.2/§7.6 MUST land before the activation in §10. Flag for operator confirmation. |

### Verdict justification

All 5 Round-1 Blockers and all 6 Round-1 Majors are FIXED with verifiable file:line evidence at HEAD. The v1-cut narrative is now coherent across `.env.example`, `proposal.md`, `design.md`, `tasks.md`, `spec.md`, `progress.md`, and `notes/operator-prereqs-checklist.md` — Round-1 internal contradictions are gone.

Two NEW Majors surfaced (N1, N2): the v1 spec-side cuts were not yet propagated to the implementation files (`scripts/backup/workers/alert.sh`, `backup-daily.sh`, `n8n/MyFinanceBackup-DispatchAlert.json`) or the operator-facing runbook (`scripts/backup/README.md`). These are CORRECTLY tracked as unchecked tasks in tasks.md (§4.2, §4.3.12, §7.6 unchecked), so the v1-cuts change is partially implemented and partially pending. The operator-prereqs-checklist (which IS finished) tells the operator to NOT provision Gmail App Password / Kuma URL — yet the worker code still tries to use them. Functional impact: graceful degradation (ntfy alone fires; Gmail+Kuma branches log "not set — skipping"); **but the Resend leg of the spec's two-channel alerting Requirement is not yet implemented at all**, so the change cannot be archived until §4.2 + §7.6 land.

The Round-2 verdict is **PASS WITH GAPS** rather than PASS because:
- The Round-1 issues raised against the cut-affected spec/proposal/design files are all resolved.
- The change-as-a-whole still has two Majors blocking archive (N1 + N2) — but these are correctly represented as unfinished `[ ]` tasks in tasks.md and the progress.md tracks them. They are NOT new regressions introduced by the pass-2 commit; they are pre-existing implementation work that the v1 cuts simply re-surfaced.
- N3–N5 are minor cleanup.
- N6 is a single clarification question with no blocking impact.

Recommend the operator land §4.2 + §7.6 + the §2.5.1/§2.5.2 README rewrite (and flip those tasks to `[ ]` if currently `[x]`) before `/opsx:archive`.

VERDICT: PASS WITH GAPS
Counts: 0 Blocker / 2 Major / 3 Minor / 1 Question
