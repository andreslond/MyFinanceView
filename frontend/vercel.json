{
  "buildCommand": "npm run typecheck && npm run lint && npm run test && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
