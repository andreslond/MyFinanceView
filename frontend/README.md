# frontend/ — placeholder

This directory will host the React 19 + Vite + TypeScript client when **Épica 3 — App / UI** kicks off. Today it intentionally contains no code so the monorepo layout already shows where the frontend will live.

## What lives here when active

- `package.json`, `vite.config.ts`, `tsconfig.json` — build manifests.
- `src/` — application code.
- `index.html` — entry point.

## Where to look before adding code here

1. [`../SPEC.md` §3.1 "Estructura del repositorio"](../SPEC.md) — the canonical monorepo layout.
2. [`../docs/frontend-standards.md`](../docs/frontend-standards.md) — code conventions (currently a placeholder; fill this before scaffolding).
3. [`../docs/design/design-system.md`](../docs/design/design-system.md) — visual language.
4. [`../docs/design/ui-handoff.md`](../docs/design/ui-handoff.md) — screens, flows, dev handoff.
5. [`../docs/api-spec.yml`](../docs/api-spec.yml) — backend contract.

## Until then

Per `docs/frontend-standards.md` §41: *do not generate frontend code for this project beyond minimal demo scripts.*
